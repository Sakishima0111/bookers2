<p>共通のlist内容↓</p>
<h1>New book</h1>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

<h1>Books</h1>
<p>ここに投稿一覧(全体)が表示される予定です。<br>
内容は投稿者のimageとtitle,Opinion</p>
